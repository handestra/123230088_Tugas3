{
  "name": "notes-app",
  "version": "1.0.0",
  "description": "``` notes-app/ ├── backend/ │   ├── server.js        ← API Express.js │   ├── notes_db.sql     ← File migrasi database │   └── package.json └── frontend/     └── index.html       ← UI (HTML, CSS, JS) ```",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "express": "^5.2.1"
  }
}
