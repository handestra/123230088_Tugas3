runtime: nodejs22
service: tugas3

# Tambahkan ini supaya hemat (untuk testing)
automatic_scaling:
  target_cpu_utilization: 0.65
  min_instances: 0
  max_instances: 1

env_variables:
  NPM_CONFIG_LEGACY_PEER_DEPS: "true"
  NODE_ENV: "production"